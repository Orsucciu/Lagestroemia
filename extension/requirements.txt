# Lagestroemia native host — optional FastAPI dependencies.
# Install with: pip install -r extension/requirements.txt
#
# These are only needed if you want to use native_host_fastapi.py
# instead of the stdlib native_host.py. The stdlib version works
# without any dependencies.
#
# To switch to the FastAPI version, set LAGESTROEMIA_USE_FASTAPI=1
# in the wrapper script (native_host_wrapper.bat / .sh).

# The OpenAI-compatible HTTP layer — handles SSE, tool calls,
# reasoning content, forward compatibility, /docs.
# https://github.com/deepset-ai/fastapi-openai-compat
fastapi-openai-compat>=0.4.0

# FastAPI + uvicorn are dependencies of fastapi-openai-compat, but
# we pin them explicitly for reproducibility.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
